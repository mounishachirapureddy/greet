apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: frontend-service
    service: frontend-service
spec:
  selector:
    app: frontend-service
  ports:
    - port: 3000
      name: http
